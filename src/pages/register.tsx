import FormPage from "../components/FormPage"
import RegisterForm from "../components/RegisterForm"

const Register = () => {
  return <FormPage Form={RegisterForm} dividerText="Kayıt" />
}

export default Register
